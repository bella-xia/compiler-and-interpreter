function add(x, y) {
    x + y;
}

function div(x, y) {
    x / y;
}

function sub(x, y) {
    x - y;
}

function multi(x, y) {
    x * y;
}

var a;
var b;
var c;
a = 1;
b = 2;
c = 3;
multi(add(sub(a, div(b, a)), c), c) == (a - b / a + c) * c;